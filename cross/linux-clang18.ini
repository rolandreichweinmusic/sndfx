# Meson native file: build the Linux host target with Clang 18.
# Alternative to GCC 14; also provides C++23 <print> / std::print (via the
# installed libstdc++ 14 headers).
#
#   meson setup build-linux --native-file cross/linux-clang18.ini
#
[binaries]
c = 'clang-18'
cpp = 'clang++-18'
